---
dotnet_deps:
  - libunwind8
  - liblttng-ust0
  - libcurl{% if ansible_distribution_version | int >= 10 %}4{% else %}3{% endif %}
  - libssl-dev
  - libuuid1
  - libkrb5support0
  - zlib1g
  - libicu-dev
